import BasicPlatform from './basic.js'
import BreakablePlatform from './breakable.js';
import BouncePlatform from './bounce.js';

export { BasicPlatform, BreakablePlatform, BouncePlatform };

export const spawnRates = {
    breakable: 10,
    bounce: 20,
}